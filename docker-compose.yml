version: "3.9"

services:
  app:
    build:
      context: .
      #flake8
      args:
        - DEV=true
      #flake8
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"